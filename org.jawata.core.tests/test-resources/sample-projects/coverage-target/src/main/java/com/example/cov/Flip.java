package com.example.cov;

/** The stability probe target — see FlipTest. */
public class Flip {

    public int flip(boolean b) {
        if (b) {
            return 1;
        }
        return 2;
    }
}
